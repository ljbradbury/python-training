#!python3

##
# Stripping whitespace
##

# With the whitespace

names = (' Languages Python C# JavaScript ')
print(names)

# Stripping the whitespace

# rstrip = removes whitespace from the right
# lstrip = removes whitespace from the left
# strip = removes whitespace from both sides

names = names.strip()
print(names)